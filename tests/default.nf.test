nextflow_pipeline {

    name "Test pipeline"
    script "./main.nf"

    test("Pipeline runs with test profile") {
        when {
            params {
                outdir = "$outputDir"
            }
        }

        then {
            assert workflow.success
        }
    }
}
